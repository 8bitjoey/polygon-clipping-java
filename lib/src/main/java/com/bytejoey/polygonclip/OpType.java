package com.bytejoey.polygonclip;

/**
 * The four boolean operation types (operation.js run() type strings).
 * Each value identifies the operation a single {@link Operation} run performs.
 */
public enum OpType {
  UNION,
  INTERSECTION,
  XOR,
  DIFFERENCE
}
